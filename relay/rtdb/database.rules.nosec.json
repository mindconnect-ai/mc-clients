{
  "rules": {
    ".read": false,
    ".write": false,

    "pairings": {
      "$code": {
        ".read": true,
        "claim": { ".write": "!data.exists()" }
      }
    },

    "tunnels": {
      "$serverId": {
        ".read": true,
        ".write": true
      }
    }
  }
}
